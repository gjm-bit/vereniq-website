import { notFound } from "next/navigation"; import { PublicShell } from "@/src/components/site-shell"; import { localArticles } from "@/src/repositories/local"; import { pageMetadata, breadcrumbJsonLd } from "@/src/lib/seo";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const slug=(await params).slug;const a=await localArticles.getPublished(slug);if(!a)return {};return pageMetadata({title:a.title,description:a.excerpt,path:`/kennisbank/${slug}`});}
export default async function Article({params}:{params:Promise<{slug:string}>}){const slug=(await params).slug;const a=await localArticles.getPublished(slug);if(!a)return notFound();const breadcrumb=breadcrumbJsonLd([{name:"Home",path:"/"},{name:"Kennisbank",path:"/kennisbank"},{name:a.title,path:`/kennisbank/${slug}`}]);return <PublicShell><script type="application/ld+json" dangerouslySetInnerHTML={{__html:breadcrumb}}/><article className="container section" style={{maxWidth:800}}><p className="eyebrow">{a.category}</p><h1 style={{fontSize:"clamp(2.7rem,5vw,4.5rem)"}}>{a.title}</h1><p className="lede">{a.excerpt}</p><hr style={{border:0,borderTop:"1px solid var(--line)",margin:"36px 0"}}/><p style={{fontSize:"1.15rem",lineHeight:1.8}}>{a.content}</p></article></PublicShell>}
