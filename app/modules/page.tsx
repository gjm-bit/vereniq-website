import { PublicShell } from "@/src/components/site-shell"; import { ModuleGrid } from "@/src/components/content"; import { localModules } from "@/src/repositories/local"; import { pageMetadata } from "@/src/lib/seo";
export const metadata=pageMetadata({title:"Modules voor verenigingen",description:"De losse onderdelen van het Meer Vereniging-platform. Gebruik alleen wat jouw vereniging nodig heeft.",path:"/modules"}); export default async function Modules(){return <PublicShell><section className="section section-soft"><div className="container"><p className="eyebrow">Onderdelen van het platform</p><h1>Alles werkt samen.</h1><p className="lede">Meer Vereniging bestaat uit verschillende onderdelen die samen één platform vormen. Modulepagina&apos;s leggen uit wat elk onderdeel doet; ze zijn geen losse betaalde add-ons.</p></div></section><section className="section"><div className="container"><ModuleGrid modules={await localModules.listPublished()}/></div></section></PublicShell>}
